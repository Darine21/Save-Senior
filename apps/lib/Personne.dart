import 'package:flutter/material.dart';
class MyPersonne extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return Container(
      width:60,
      height:60,

      child: Image.asset('assests/images/52033.png'),
    );
  }
}